{
  "name": "layouter-lab-back",
  "version": "1.0.0",
  "packageManager": "pnpm@7.5.1",
  "scripts": {
    "build": "echo \"ok $PORT\"",
    "test": "echo \"Error: no test specified\" && exit 0",
    "vercel dev": "directus start",
    "dev": "directus start",
    "run": "directus start",
    "start": "directus start",
    "lint": "echo \"Error: no lint\" && exit 0",
    "type-check": "echo \"Error: no type check\" && exit 0",
    "add-extension": "cd packages;npm init directus-extension",
    "create-snapshot": "npx directus schema snapshot ./snapshots/\"$(date \"+%F\")\"-snapshot-\"$(date \"+%s\")\".yaml",
    "migrate": "npx directus database migrate:latest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cockroachdb": "^1.0.3",
    "directus": "^9.18.1",
    "directus-extension-wpslug-interface": "^1.1.0"
  }
}
